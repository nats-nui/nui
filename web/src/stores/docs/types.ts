
export enum DOC_TYPE {
	EMPTY = "emp",
	CONNECTIONS = "cns",
	SERVICES = "srv",
	MESSAGES = "msg",
}

export enum POSITION_TYPE {
	LINKED = "lnk",
	DETACHED = "dtc"
}

export enum DOC_ANIM {
	INIT="init",
	SHOW="show",
	EXIT="exit",
	DRAGGING="dragging",
	EXITING="exiting",
	SHOWING="showing",
}